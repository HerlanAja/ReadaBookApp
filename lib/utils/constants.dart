import 'package:flutter/material.dart';

// Warna yang digunakan dalam aplikasi
const Color primaryColor = Color(0xFF274a99);
const Color secondaryColor = Color(0xFF03DAC6);
const Color defaultBackgroundColor = Color(0xFFFFFFFF);
const Color accentColor = Color(0xFF03DAC5);

// Ukuran font
const double defaultFontSize = 14.0;
const double titleFontSize = 20.0;

// Ukuran padding dan margin
const double defaultPadding = 16.0;
const double smallPadding = 8.0;
const double largePadding = 32.0;

// Nama aplikasi dan database
const String appName = 'Book Management App';
const String dbName = 'books.db';
